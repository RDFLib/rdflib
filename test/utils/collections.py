# import collections


# # def HashableMapping(mapping):
# #     """A mapping that is hashable.

# #     This is useful for testing that a mapping is hashable, since
# #     dictionaries are not hashable.

# #     Args:
# #         mapping: A mapping.

# #     Returns:
# #         A hashable mapping.
# #     """
# #     return collections.namedtuple('HashableMapping', mapping.keys())(**mapping)
