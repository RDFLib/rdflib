methods to add:

- getContexts(model, contextUri)
- get(..., context) -- parameterize some methods with an optional context name
- an options vocab which is owl compat

Typing the HTTP interface bits:

foo=uri:bar
foo=<bar>
foo-type=bar

Ugly...